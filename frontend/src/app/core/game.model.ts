export interface Game {
  id: number;
  studioId: number;
  title: string;
  shortPitch: string;
  genre: string;
  currentStage: string;
  validationStatus: string;
  targetPlatforms: string;
  createdAt: string;
}
